---
class:      capitolo
lang:       en
layout:     manuale
og_img:     "/assets/img/og-img/ereditarieta.jpg"
permalink:  /en/inheritance
quote:      "A parent class can disown a child class, if it turns out bad, but a child class cannot disown its parent class, because it owes it more than it could ever repay — and, in particular, owes it its own existence."
status:     pubblicato
title:      Inheritance
translation_reference: ereditarieta
og_desc:    "The Karma of virtual functions - Science is fleeting; Myth is immortal - Why 13 and 17 are unlucky numbers - What Art is - The rules of Ikebarba"
---

<!-- Etica Nicomachea 1163b -->

Inheritance, that is, the ability to create genealogies of classes, is C++'s main distinguishing feature.

As we saw in the <a href="/en/classes-objects#dottrina" class="xref">previous lesson</a>, a hypothetical `Fish` class would need different attributes depending on what it's being used for.
In a language like *C*, which doesn't allow inheritance, you'd therefore have to set up two different data structures for each case:

```
enum Water  { freshwater, saltwater };
enum Sex    { male, female };
enum Color  { red, blue, green, silver };

struct FoodFish {
    Sex    _sex;
    char*  _species;
    float  _price;
    time_t _catch_date;  
    int    _weight;
    bool   _edible;
    char*  _fishing_area;
    ...
};

struct  AquariumFish {
    Sex    _sex;
    char*  _species;
    float  _price;
    time_t _purchase_date;  
    Color  _color;
    Water  _water;
    ...
};
```
and different functions to manage the data:

```
void manageFoodFish(struct FoodFish& fish);
void manageAquariumFish(struct AquariumFish& fish);
```

This means that if you've already written (and verified, fixed, and tested) a management program for a fishmonger, to turn it into a management program for selling aquarium fish, you'd have to rewrite (and re-verify, re-fix, and re-test) all the code, even though part of the data involved is the same.
Inheritance, by contrast, lets you isolate the characteristics common to both contexts into a single class, and *derive* from this base class two specialized classes:

```
{% include_relative src/inheritance-single.cpp %}
```

This approach has two upsides: the first is that you won't need to repeat the testing, debugging, and validation phases for the functions common to both systems, since they'll already have been verified during the development of the first system; the second is that, by reusing part of the code, you'll be able to spot and fix any bugs that slipped through the first round of testing, or improve the behavior of the shared functions, benefiting both systems.

<hr id="tipi">

In C++, inheritance can be either *single* or *multiple*, depending on whether the new class inherits its characteristics from one or more pre-existing classes:

```
class Son : public Mom
{
    /** Single inheritance */
};

class Son : public Mom, private Dad
{
    /** Single inheritance */
};
```

By contrast, it's not allowed (nor would it make sense) for a class to inherit twice from the same base class:

```
class Error : public Mom, public Mom
{
	...
};
```

The `Mom` and `Dad` classes can be called *base classes*, *subclasses*, *fundamental classes*, or *subtypes*; the `Son` class can be called either the *derived class*, the *superclass*, or the *supertype*.
The instruction:

```
class FoodFish : public Fish {
    ...
};
```
declares the `FoodFish` class as derived from the `Fish` class.
The <a href="/en/classes-objects#specificatori-accesso" class="xref">access specifier</a> between the two class names defines the visibility of the base class's data inside the child class:

```
class B : public A
{
    // all of A's members keep
    // their original visibility in B
};

class B : protected A
{
    // all of A's public members become
    // protected members of B; protected and private
    // members keep their original visibility
};

class B : private A
{
    // all of A's members, whatever their
    // visibility, become private members of B;
};

```

In the absence of an access qualifier, the compiler treats all the data of a class declared with the `class` keyword as private, and all the data of a class declared with the `struct` keyword as public:

```
{% include_relative src/inheritance-access.cpp %}
```

If you compile this code, you get an error message:

```
> g++ src/cpp/inheritance-access.cpp -c -o src/out/example
src/cpp/inheritance-access.cpp:25:5: error: cannot cast 'Son'
    to its private base class
      'Mom'
    son.a++;    
    ^
src/cpp/inheritance-access.cpp:17:16: note: implicitly declared
    private here
class Son : Mom {
               ^~~~~
src/cpp/inheritance-access.cpp:25:12: error: 'a' is a private
    member of 'Mom'
    son.a++;    
           ^
src/cpp/inheritance-access.cpp:17:16: note: constrained by
    implicitly private inheritance here
class Son : Mom {
               ^~~~~
src/cpp/inheritance-access.cpp:8:9: note: member is declared
    here
    int a;
        ^
2 errors generated.
```


<hr id="costruttore">

This is the declaration of the derived class `FoodFish`'s constructor:

```
FoodFish(Sex sex, float price, const char* species)
: Fish(sex, price, species) {
    _edible = true;
}
```

The second line is the class's <i id="lista-inizializzazione">initialization list</i>, and contains the constructors of the base classes.
When you instantiate an object of a derived class, the system first calls the base classes' constructors, then the child class's own constructor.
This way, the derived class's constructor is guaranteed to be working on correctly initialized member data.
Using the base class's constructor to initialize shared data is necessary for two reasons: the first is that some of the base class's data might be `private`, and therefore inaccessible to the derived class; the second is that this achieves <a href="/en/program-structure#coupling" class="xref">low coupling</a> between the base class and the derived class, so that if the internal implementation of the base class's constructor ever needed to change (while keeping the interface unchanged), the code of its derived classes wouldn't need to change too.
The order in which the base classes' constructors are called during the object's initialization depends on the order in which they appear in the child class's constructor.
Let's look at another example, a bit more complex than the previous one, which also shows how static member data and functions work:

```
{% include_relative src/inheritance-multiple.cpp %}
```

The difference from the previous code is that in this case we've isolated the data related to the fish's cost into a separate class, and the derived classes now inherit not from one base class, but from two.
If you compile and run this code, you get:

```
> g++ src/cpp/inheritance-multiple.cpp -o src/out/example
> src/out/example                                         
Fish constructor
Item constructor
FoodFish constructor
Item constructor
Fish constructor
AquariumFish constructor
aquarium fish: Paracheirodon
food fish: Dicentrarchus labrax
base class: item
items created: 2
fish created:    2
AquariumFish destructor
Fish destructor
Item destructor
FoodFish destructor
Item destructor
Fish destructor
```

As you can see, the order in which the base classes' constructors are called mirrors the order in which they're listed in the initialization list, while the order of the destructors is reversed.
If the derived class's constructor doesn't specify an order for calling the base classes' constructors, the calling order is given by the order in which the base classes appear in the child class's declaration:

```
{% include_relative src/inheritance-constructor-order.cpp %}
```

If you compile and run this code, you get:

```
> g++ src/cpp/inheritance-constructor-order.cpp -o src/out/example
> src/out/example                                                   
Dad constructor
Mom constructor
Son constructor
Mom constructor
Dad constructor
Daughter constructor
```

Another thing to notice, in this code, is that the `getType` function is present both in the `Item` base class and in the two derived classes `FoodFish` and `AquariumFish`.
For this reason, when you call `getType` from an instance of either derived class, as in:

```
cout <<  fish1.getType() << ": " << fish1.getSpecies()
cout <<  fish2.getType() << ": " << fish2.getSpecies()
```

the value returned is that of the child class's function.
To get the base class's value, we have to specify its name in the instruction, as in:

```
cout << "base class: " << fish1.Item::getType() << endl;
```

<hr id="classi-virtuali">

Let's complicate things a bit.
Imagine a `Son` class deriving from the `Mom` and `Dad` classes, which in turn derive from the `Person` class.
If we called a function of the `Person` class from an object of class `Son`, which one would get called — the one inherited from `Dad`, or the one inherited from `Mom`?

```
{% include_relative src/inheritance-virtual-base-classes.cpp %}
```

Actually, neither, because this code produces an error:

```
src/cpp/inheritance-virtual-base-classes.cpp:30:11:
error: non-static member 'getClass' found in multiple base-class
      subobjects of type 'Person':
    class Son -> class Dad -> class Person
    class Son -> class Mom -> class Person
    cain.getClass();
          ^
src/cpp/inheritance-virtual-base-classes.cpp:13:10:
note: member found by ambiguous name lookup
    void getClass(){
         ^
1 error generated.
```
You can avoid this kind of problem by declaring the `Person` class as a *virtual* base class of both `Mom` and `Dad`:

```
class Dad : public virtual Person {
};

class Mom : virtual public Person {
};
```

This way, the `Son` class will inherit all the members specific to the `Mom` and `Dad` classes, but only a single copy of the methods and attributes of the virtual `Person` class that both of them contain.

<hr id="classi-puntatori">

As I've told you, defining a new class is equivalent to defining a new data type, which the compiler will treat the same way as the language's primitive data types.
This means, for example, that if we want, we can create an array of objects of class `Fish` the same way we'd create an array of `int` or `char`:

```
/** Array of objects of class Fish */
Fish aquarium[10];
```

There's only one limitation: since every element of an array must be initialized when it's created, the class must have a default constructor.
If we decided to create an array of objects of the `Point` class we saw <a href="/en/classes-objects#attributi-metodi" class="xref">in the last lesson</a>, the array's elements would have to be initialized explicitly:

```
Point spline[3] = { Point(3,5), Point(0,0), Point(7,7) } ;
```

We can also add objects belonging to a base class's derived classes into an array of objects of that base class:

```
Person family[3] = { Dad(), Mom(), Son() } ;
```

Conversely, base class objects cannot appear in an array of objects of a derived class, and an instruction like the one below will produce an error:

```
Son class[4] = { Son(), Son(), Person(), Son() } ;
```

Just as, in chess, the queen can move like a rook, but a rook cannot move like a queen, an object of type `Person` doesn't contain all the information of an object of type `Son`, and so cannot be used in its place.
The same reasoning that applies to arrays also applies to pointers.
A pointer to objects of type `Person` can be assigned an object of type `Son`, while the reverse operation will cause a compile error:

```
Person *ptrP = new Son() ;  // OK
Son    *ptrF = new Person();  // ERROR!
```

The compiler is able to understand the relationship between a derived class and its base class, and can therefore establish a coercion path from the object's type to the pointer's type, but it has no way of accessing the members or functions of a derived class from a base class object.
We said <a href="/en//data-types#puntatori" class="xref">earlier on</a> that pointers are like masks that isolate certain sequences of bits, whose size varies depending on the pointer's type.
The same reasoning applies to classes too: a base-class pointer associated with a derived-class object will only "see" the data and functions of its own class:

```
{% include_relative src/inheritance-pointers.cpp %}
```

If you compile and run this code, you get:

```
> g++ src/cpp/inheritance-pointers.cpp -o src/out/example
> src/out/example
Mom
Person
```

There's no error: the `getClass()` function invoked in the second output instruction isn't, as you might expect, that of the `Mom` class, to which the object belongs, but that of the base class, which is the only one the program can access through a pointer to objects of type `Person`.
This (correct) behavior of the program becomes particularly risky if the class has a destructor:

```
{% include_relative src/inheritance-destructors.cpp %}
```

No compiler will ever give you an error for this code, but the destructor called, in all three cases, will be that of the base class `Dad`, with consequences ranging from problematic to disastrous.

<hr id="funzioni-virtuali">

For a member function of a derived class to be callable even through pointers to base classes, it must be declared `virtual`.

```
{% include_relative src/inheritance-virtual-functions.cpp %}
```
The output of this code is:

```
> g++ src/cpp/inheritance-virtual-functions.cpp \                 
    -o src/out/example
> src/out/example                                 
Mom
Mom
```

*Virtual functions* are functions that get called based on the class of the object they belong to, regardless of the type of the reference or pointer being used.
This is made possible by a mechanism called *dynamic binding* or *late binding*, which consists of postponing the *linking* of functions until the program actually runs, unlike what happens with normal member functions, which are linked to the code at compile time &mdash; so-called *early binding*.
In practice, it works like this: the addresses of all functions declared `virtual` are stored in an internal table, and only when one of these functions is called by the program does the system look up its address, performing the *linking* in real time.
You can see for yourself that using virtual functions, besides causing a slight delay in the program's execution time, since the function's address has to be looked up, also uses up some of the system's resources to store the address table — so, as with everything, it's best not to overdo it.
The rules governing the use of virtual functions are:

- the versions of the functions in derived classes must have the same return type and the same parameters as the base class's version: if not, the compiler treats the two functions as different, and the "virtual" effect is lost;

- a `virtual` function cannot also be `static`: the very concept of a virtual function requires a link between an object and a function; static functions are independent of the objects of their class, so the two are incompatible;

- a function can only be declared `virtual` in the base class: it's not possible to make that declaration in a derived class;

- you can repeat the `virtual` specifier in derived classes too, but it isn't necessary: you can see this in the example, where the second version of the `getClass()` function doesn't have the `virtual` keyword in front of it;

- using the scope resolution operator inevitably cancels out the effect of virtual functions.

<hr id="classi-astratte">

It's possible to declare a virtual function in the base class without defining its behavior, using the syntax:

```
virtual <type> functionName([arguments]) = 0 ;
```    

This kind of function is called a <i id="funzioni-virtuali-pure">pure virtual function</i>, and it makes the class it belongs to an *abstract class*.
Abstract classes are generic classes that can be used as ancestors for a lineage of specialized classes, but that cannot be used directly.
The rules that apply to abstract classes are:

- any class with at least one pure virtual function is considered abstract;
- pure virtual functions are inherited by derived classes as pure virtual functions, so, if a derived class doesn't redefine a pure virtual function of its base class, the compiler will treat it as an abstract class too;
- abstract classes cannot be used as function arguments or return types;
- abstract classes cannot be the type of an object or of an explicit conversion.

Given an abstract class `Mammal`, the following instructions would cause compile errors:

```
void func(Mammal m);   // cannot be arguments..
Mammal func();         // nor return values..
ptr = (Mammal*)ptr2;   // nor the type of a conversion.
Mammal chicken;        // nor the type of an object
```

It's possible, though, to declare a pointer or a *reference* to an abstract class, and use them to create arrays or queues that can be used with instances of different classes:

```
{% include_relative src/inheritance-abstract-classes.cpp %}
```

If you compile and run this code, you'll get:

```
> g++ src/cpp/inheritance-abstract-classes.cpp -o src/out/example
> src/out/example                                                
dog
female
Scylla
dog
female
```

The `Mammal` base class defines only an abstraction, leaving it to its derived classes to define attributes and methods specific to each particular species.
In the same way, the `getSpecies` function defines only a concept, not an algorithm; it's up to the individual derived classes to redefine the function's behavior, adapting it to their own needs.
It is possible, however, to also define a behavior for pure virtual functions; for the `Mammal` class it might look something like this:

```
inline void Mammal::getSpecies()
{
    cout << "none" << endl ;
}
```

Since objects of class `Mammal` cannot exist, though, the base version of the `getSpecies` function could only be called by making use of the `::` operator:

```
myDog.Mammal::getSpecies();
dog.Mammal::getSpecies();
```
<hr id="dottrina">

Most of the examples and things I've told you in this lesson I took from the C++ programming manual Master Canaro wrote in the last Century, adapting them to this context.
The original example of virtual functions, for instance, looked like this:

```
{% include_relative src/inheritance-canaro-code.cpp %}
```

Being code written for the Microsoft environment of 1995<!-- con i nomi dei file di otto caratteri al massimo: sembra di parlare della preistoria.. -->, if you tried to compile it now, with the GNU compiler, you'd get a long series of errors:

```
> g++ src/cpp/inheritance-canaro-code.cpp -o src/out/example
src/cpp/inheritance-canaro-code.cpp:8:10:
fatal error: 'iostream.h' file not found
#include <iostream.h>
         ^~~~~~~~~~~~
src/cpp/inheritance-canaro-code.cpp:13:8:
error: use of undeclared identifier 'cout';
did you mean 'std::cout'?
        { cout << "I am an object of class A \n" ; }
          ^~~~
          std::cout
/Library/Developer/CommandLineTools/usr/bin/
../include/c++/v1/iostream:54:33: note:
'std::cout' declared here
extern _LIBCPP_FUNC_VIS ostream cout;
                                ^
src/cpp/inheritance-canaro-code.cpp:21:8:
error: use of undeclared identifier 'cout'

did you mean 'std::cout'?
        { cout << "I am an object of class B \n" ; }
          ^~~~
          std::cout
/Library/Developer/CommandLineTools/usr/bin/
../include/c++/v1/iostream:54:33: note: '
std::cout' declared here
extern _LIBCPP_FUNC_VIS ostream cout;
                                ^
src/cpp/inheritance-canaro-code.cpp:24:1:
error: 'main' must return 'int'
void main()
^~~~
```

The teaching value of this code, though, is unchanged.
The *Karma* of the two objects is determined by their classes' declaration, which leaves them no choice but to do what they were created to do.
The output of the functions, the style of the comments, or the fact that in one case the classes are called `A` and `B`, while in the other they're called `Person` and `Mom`, are just formal differences that don't affect the example's *Dharma*, which is to illustrate the behavior of virtual functions.
If, instead of the new code, I had used Master Canaro's original, you would have understood it just as well; maybe even better, because the new code seems better to me, but that doesn't mean it is to you too.
The same principle also applies to the Universe.
Just as objects within the same program occupy different positions in memory depending on when the program is run, sentient beings can manifest in different places and times across different cycles of existence.
Each of us is an orchestra player who has been handed a score.
We can play it more or less well, or not play it at all, hiding within the swell of the orchestra, but our worth is only &mdash; forgive the pun &mdash; instrumental, because what matters isn't us: it's the music; and this isn't the first, nor will it be the last, time we play it.
I, here, now, with my nose, my mouth, and my hair, am teaching you what I know about C'hi++, and you, with your own nose, your own mouth, and your own hair, are writing it down in your book, but the very same information we're passing on and perpetuating could be passed on and perpetuated by entirely different people, at another time, or on another Planet, in another cycle of the Universe.
In all likelihood we've already had this exact conversation in the past, and we'll have it again in the future.
Perhaps we won't use the same words; perhaps we'll have different names, perhaps you'll be the master and I the disciple, but our friendship will be the same, because that's part of our class's declaration; it cannot, and must not, change.

> {{site.data.citations.mulino-amleto.mito}} <a class="nota" href="/en/notes#fn-miti" id="miti">1</a>

Every age has its heroes and its demons; the memory of their battles gives rise to myth.
Science is fleeting: we know little about the scientific knowledge of past peoples<!-- esempi? -->, while we know their myths well, because myth is immortal; Science isn't, unless it's absorbed by myth and turned into legend or superstition.
Master Canaro, for instance, was convinced that the superstition around the numbers 13 and 17 arose from observing the behavior of locusts,<!-- il nome corretto, dice Dawkins, è: “cicale periodiche”, ma, per chiarezza, preferisco utilizzare quello più noto --> which, depending on the species, spend either thirteen or seventeen years underground in a larval stage<!-- anche qui, Ricky precisa che il termine tecnico è: “ninfe” -->, then all emerge together and spend their short lives devastating crops.
The fear of the numbers 13 and 17, in his view, was scientific knowledge handed down across time and space as superstition, by farming cultures that periodically watched their harvests get destroyed.

<hr id="arte">

Art is the engine of myth.
A self-fueling engine, like the Sun, because it feeds on epic events and in turn generates them, inspiring heroes to deeds worth remembering.
The word *art*, like *love* for that matter, is one of those words people use often but never precisely define, because they're considered self-explanatory concepts that need no defining.
That's wrong: as we've seen, all words, even the most common ones, can be interpreted in different ways.
In <a href="https://canaro.net/saggi/amore-e-arte.html" target="canaro">an essay he wrote in his youth, on Love and Art</a>, Master Canaro wrote that:

 > Art is the trace of Man's journey toward God

 later specifying that, by the term "God" (another word each of us interprets differently), he meant the meaning of Existence.
Some years later, though, looking at photos of tarts made by chef Gianluca Fusto,<a class="nota" href="/en/notes#fn-fusto" id="fusto">2</a> he realized his definition was imperfect, because it didn't include, or at least left on the margins, those art-objects that didn't fall within the canonical artistic categories.
He therefore changed his definition of Art to:

> Art is the trace of Man's journey toward Perfection

This wasn't a contradiction, but a clarification, since for him &mdash; as for us, for that matter &mdash; the pursuit of perfection was, in fact, the meaning of Existence, and therefore, God.
This episode from Master Canaro's life has the peculiarity of demonstrating the very principles it states: the first is that we shouldn't be afraid to question our own ideas, if we realize they're wrong or incomplete; the second is that, if we approach life the right way, everything we do will be Art, not just flower arranging or the Tea ceremony.
In life there are no first-class moments, when we do the things we enjoy, and second-class moments, when we do what needs to be done: every instant matters.
To underscore this precept, Master Canaro defined an aesthetic for hanging laundry on a drying rack, and, so as not to risk being taken too seriously, called it <i id="ikebarba">Ikebarba</i>.
The principles of Ikebarba, as Master Canaro formulated them, are:

<blockquote>
<p>
<b>Ikebarba is made for man, not man for Ikebarba.<a class="nota" href="/en/notes#fn-shabbat" id="shabbat">3</a></b>  
Ikebarba should not be a burden for whoever practices it, but a joyful duty.
The laundry has to be hung out to dry regardless; the time it takes to do so sloppily or mindfully is more or less the same, but a well-made Ikebarba will produce dry laundry in less time, and will make the sight of the drying rack less annoying.
</p>
<p>
<b>Ikebarba begins in the shop.</b><br />
Garments of different colors, or in loud colors, are hard to match chromatically; it's therefore best to buy clothes in muted tones, ideally matching one another, to make composing them on the drying rack easier and more elegant.
Be careful, though: a military- or Maoist-style wardrobe, with identical garments all the same color, is an excess to avoid, because it would make the composition (and not only that) monotonous.
</p>
<p>
<b>Ikebarba shuns clothespins.</b>
Clothespins are as useful as psychoanalysis: it's balance that should keep the laundry on the line, not an external coercive force.
The various garments should be arranged on the line so that the weight on one side balances the weight on the other.</p>
<p style="display:block">
There are three kinds of Ikebarba:
<ul style="padding:1rem 1rem 0">
<li><b>chromatic:</b> when the laundry is arranged on the rack by color;</li>
<li><b>functional:</b> when the garments are arranged according to their respective drying times, placing heavy items on the outside, where they get more air, and lighter or synthetic ones on the inside;</li>
<li><b>perfect:</b> when the aesthetic and functional aspects merge into a harmonious whole.</li></ul></p></blockquote>
As you can probably guess, Master Canaro's intentions were largely ironic (he confessed to me that the discipline's first formulation was born as an attempt to disguise his obsession with tidiness), and for a long time he was unsure whether to include it in the body of the Doctrine.
He decided to do so once he realized his own reservations stemmed from exactly the kind of prejudice Ikebarba was meant to counter.
As Banzan and Paul Simon teach us, Truth is everywhere, if we know how to look for it, even on subway walls or in market stalls.
In any case, Ikebarba can genuinely benefit those who practice it.
First of all, it tempers the harmful effect of the Age-Old Dichotomy and its servants *Marketing* and *Fashion*, which push us to buy clothes we don't need and will only use for a short time.
By reducing the chromatic variation of the laundry, it also reduces the number of weekly washes, and with it the need for electricity, water, detergents, and plastic.
Even the rejection of clothespins has a functional value of its own: if garments are hung draped over the lines, the water in the fabric will tend to run down both sides, reducing drying time.
Admittedly, the fact that our Order's Rule requires us to wear white shirts doesn't solve the pollution problem, but, as Master Canaro used to say:

> Fur is made of hair

and until our species learns to make more responsible use of its gonads, all we can do is compensate as best we can for the problems tied to overpopulation.
